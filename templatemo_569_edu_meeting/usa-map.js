document.addEventListener('DOMContentLoaded', () => {
    const states = document.querySelectorAll('.state');

    //Handle Hover
    state.addEventListener('mouseenter', () => {
        console.log("Entering:", state.id);
    }); 
});